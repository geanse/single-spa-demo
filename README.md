# Single SPA root config Demo

This is an example of a [Single SPA root config](https://single-spa.js.org/docs/configuration) that loads Single SPA apps.

```
$ yarn install
$ yarn run start
```

> Created by https://single-spa.js.org/docs/create-single-spa/
